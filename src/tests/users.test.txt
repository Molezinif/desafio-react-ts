import { render, screen } from '@testing-library/react'
import React from 'react'
import UserCard from '../components/users/UserCard'

describe('userCard.tsx', () => {
  it('Should be render', () => {
    const wrapper = render(
      <UserCard
        name={''}
        email={''}
        phone={''}
        username={''}
        street={''}
        company={''}
      />
    )
  })
})
